# Configuración de desarrollo local
SECRET_KEY=dev-secret-key-change-in-production

# Base de datos PostgreSQL local (Docker)
DB_NAME=quiniela_dev
DB_USER=postgres
DB_PASSWORD=dev_password123
DB_HOST=localhost
DB_PORT=5432

# Cambiar a True para usar SQLite durante el desarrollo
USE_SQLITE=False
SQLITE_PATH=quiniela.db

# Environment
FLASK_ENV=development
FLASK_DEBUG=True